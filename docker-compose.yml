version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cpms_app
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - ./:/var/www
      - /var/www/node_modules
    ports:
      - "8000:8000"
    depends_on:
      - db
    environment:
      APP_ENV: local
      APP_KEY: ${APP_KEY}
      DB_CONNECTION: mysql
      DB_HOST: db
      DB_PORT: 3306
      DB_DATABASE: cpms
      DB_USERNAME: cpms
      DB_PASSWORD: root
    networks:
      - cpms
    
  node:
    image: node:20-alpine
    container_name: cpms_node
    working_dir: /var/www
    volumes:
      - ./:/var/www
      - /var/www/node_modules
    ports:
      - "5173:5173"
    command: npm run dev
    networks:
      - cpms


  db:
    image: mysql:8.0
    container_name: cpms_db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: cpms
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: cpms
      MYSQL_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - dbdata:/var/lib/mysql
    networks:
      - cpms

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: cpms_phpmyadmin
    restart: unless-stopped
    environment:
      PMA_HOST: host.docker.internal
      PMA_PORT: 3306
      MYSQL_ROOT_PASSWORD: root
      PMA_USER: cpms
      PMA_PASSWORD: root
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8080:80"

volumes:
  dbdata:

networks:
  cpms: